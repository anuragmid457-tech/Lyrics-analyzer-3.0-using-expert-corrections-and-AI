# LYRIQ prototype - Python 3.12
#
#   python -m venv .venv
#   .venv\Scripts\Activate.ps1
#   python -m pip install --upgrade pip
#   pip install -r requirements.txt
#
# .env needs GOOGLE_API_KEY and DATABASE_URL.

Flask>=3.0,<4.0
python-dotenv>=1.0,<2.0
gunicorn>=21.2

# analyser stack. The <0.4 caps matter: langchain-google-genai 4.x requires
# langchain-core >= 1.3, which cannot coexist with the 0.3 line these files
# are written for.
langchain>=0.3,<0.4
langchain-core>=0.3,<0.4
langchain-community>=0.3,<0.4
langchain-google-genai>=2.0,<3.0

# PyPDFLoader's backend, used by detect_emotion()
pypdf>=4.2

# PostgreSQL. [binary] ships a compiled wheel, so nothing builds on Windows.
psycopg[binary]>=3.1